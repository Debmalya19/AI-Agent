# Core Flask dependencies
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-JWT-Extended==4.5.3
Flask-CORS==4.0.0
Flask-Migrate==4.0.5
Flask-SocketIO==5.3.6

# Database dependencies
SQLAlchemy==2.0.23
psycopg2-binary==2.9.7  # PostgreSQL adapter
# sqlite3 is part of Python standard library, no need to install via pip

# Redis dependencies
redis==5.0.1
hiredis==2.2.3  # High-performance Redis parser

# HTTP client dependencies
requests==2.31.0
httpx==0.25.2  # Async HTTP client
aiohttp==3.9.1  # Alternative async HTTP client

# Authentication and security
PyJWT==2.8.0
cryptography==41.0.7
bcrypt==4.1.2
passlib==1.7.4

# Data processing and validation
pydantic==2.5.2
marshmallow==3.20.1
validation==0.1.0

# Async and concurrency
asyncio-mqtt==0.16.1
aioredis==1.3.1
celery==5.3.4  # Task queue (optional)

# Monitoring and logging
prometheus-client==0.19.0
structlog==23.2.0
loguru==0.7.2

# Configuration and environment
python-dotenv==1.0.0
click==8.1.7
PyYAML==6.0.1

# Date and time handling
python-dateutil==2.8.2
pytz==2023.3

# JSON and data serialization
# orjson==3.9.10  # Fast JSON library - requires Rust
msgpack==1.0.7

# WebSocket support
python-socketio==5.10.0
python-engineio==4.7.1

# Testing dependencies (development)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
factory-boy==3.3.0

# Development tools
black==23.11.0  # Code formatter
flake8==6.1.0  # Linting
mypy==1.7.1  # Type checking
isort==5.12.0  # Import sorting

# Performance and optimization
numpy==1.25.2  # For numerical operations
pandas==2.1.4  # For data analysis (if needed)

# Email support (for notifications)
email-validator==2.1.0

# File handling
Werkzeug==2.3.7

# Environment and deployment
gunicorn==21.2.0  # WSGI server
gevent==23.9.1  # Async support for gunicorn

# Optional: GraphQL support
graphene==3.3
graphene-sqlalchemy==3.0.0
Flask-GraphQL==2.0.1

# Optional: API documentation
flasgger==0.9.7.1  # Swagger/OpenAPI

# Optional: Rate limiting
Flask-Limiter==3.5.0

# Optional: Caching
Flask-Caching==2.1.0

# Optional: Background tasks
rq==1.15.1  # Redis Queue (alternative to Celery)

# Optional: Metrics and APM
psutil==5.9.6  # System metrics

# Optional: Health checks
py-healthcheck==1.10.1

# Version pinning for security
setuptools>=65.5.1
pip>=23.0
wheel>=0.38.0